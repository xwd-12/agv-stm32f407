# AGV 智能搬运机器人 — 开发记录

## 当前状态 (2026-06-23)

### 硬件
- STM32F407 + 4 轮差速驱动 + 4-DOF 机械臂
- OpenMV 摄像头 (USART3, 115200)
- 5 路巡线传感器
- SD 卡 ✅ (已购买, AI 模型已训练部署)

### 已完成
- [x] AI 模型训练 + 部署 (MobileNetV2 α=0.35, int8量化, 三分类)
- [x] QR + AI + 颜色对准 + 抓取 全流程联调
- [x] 三物体依次抓取演示视频录制
- [x] 视觉伺服双 PID 框架 (visual_servo.c, 278行)
- [x] 视觉伺服 PID 串口调参命令 (vkp/vki/vkd/vkp2/vki2/vkd2/vdist/vcx/vpid)
- [x] LLM PID Tuner 框架就绪 (stm32f407_openmv 双控制器 profile)
- [x] 6 状态机全部实现 (ArmSM / TaskNav / TaskQueue / VisionTask / AI pipeline / VisualServo)

### 控制参数
| 参数 | 值 |
|------|-----|
| 巡线 base_speed | 180 |
| 巡线 Kp/Ki/Kd | 180 / 2.0 / 2.0 |
| 转向钳制 | ±600 |
| 右后轮偏置 | -20 (编码器坏, 开环) |
| 视觉伺服默认参数 | lat: Kp=3.0 Ki=0.05 Kd=0.5 / long: Kp=2.0 Ki=0.02 Kd=0.3 |
| 视觉伺服默认目标 | cx=160, dist=15cm |

---

## 开跑整流程 (自动启动, 2026-06-23)

**上电后无需任何串口命令自动开跑**:

```
上电 → LED 三闪 (MCU 就绪)
  → 巡线出发 (OpenMV=QRCODE)
  → 十字路口 (5 路全踩, cross_dock_enable=1 默认开启)
  → 右拐 → 脱线对准 → AprilTag 倒车对接 → 挂钩 (trailer_docked=1)
  → 巡线到 Station 0 → QR → AI/COLOR 识别 → 抓取 3 件 → 放从车
  → 巡线到 Station 1 → 从车取件 → 交付 → 全部完成 LED 三闪
```

- **boot 默认**: `cross_dock_enable=1` (十字路口自动对接开), `crossdock 0/1` 可随时切换。
- **`run`** 一键重置整个流程: AI pipeline + dock 状态机 + 工位计数全部清零 → 回到巡线, 下个十字路口重新对接。
- **`restart`** 与 `run` 等价 (现场备用一键重置)。
- 观察: 串口可看到 `AGV RUN FLOW READY` 开机横幅 + `DOCK: ...` 对接各阶段 + `Station %d: ...` 工位日志; `dockstat` / `trace` 可实时查看对接状态机。

### 调试命令 (2026-06-23 新增一批)

| 命令 | 用途 |
|------|------|
| `flow` | **全流程状态一屏 dump**: 模式/使能/十字开关 + dock_state/turning/lost/tune + qr_phase/工位计数/类别 + 电机速度/腰座角 |
| `ls` | 巡线 5 路传感器: 消抖后状态 `[R1 R2 C L2 L1]` + 原始电平 + `all_on`(十字路口检测) — **现场调右拐必用** |
| `dock_force <0-6>` | 强制跳转 dock 状态机 (1=右拐需自动置 turning) |
| `arm` | ArmSM 状态 + 5 路舵机当前角度/忙闲 (0-4 含挂钩) |
| `pid_all` | 4 路电机速度 PID + 位置 PID + 巡线 PID + 弯道计数 一屏 |
| `enc_all` | 4 路编码器 pos/spd/max/dir |
| `uptime` | 开机时长 (ticks 与秒) |

### ⚠️ 巡线传感器极性: 黑线=GPIO高, 切勿再反转! (2026-06-23 实证)

**结论**: 传感器输出是**高电平有效** — 黑线 → GPIO **高** → `states=1` = 检测到线,
`line_sensor.c` 用 `!= 0` 是对的, **不要再改**。`line_sensor.h` 注释已改为
"active-high: 黑线=GPIO高=1=检测到线"。

**坑**: "检测到黑线灭灯" 是**模块指示灯**灭, 不代表输出电平低 (指示灯驱动与输出反相)。
曾据此把 `!= 0` 误改成 `== 0`, 导致:
- 十字路口 5 路全黑 → GPIO 全高 → `states` 全 0 → `AllOn()` 永不触发 → 不检测不停车;
- 巡线把全黑误判"丢线"滑行 → 轮子/编码器乱动 (用户实测: "五路全灭但没检测到没停, 编码器乱转")。
已改回 `!= 0`, 同步恢复 `ls` 命令原始电平打印。

### 十字路口原地右转90° 方案 (2026-06-23, 最终版, 实车通过)

**检测到十字 → 停车 → 原地差速右转90° → 停 → 转腰座对接 (dock_state 2)**。实车已转起来, 仅剩角度微调。

关键修复链 (按时间):
1. 传感器极性: **黑线=GPIO高** (`!=0`, 勿反转; "灭灯"是模块指示灯与输出反相)。
2. 停车必 `PID_Reset` 4 个电机速度 PID (否则右轮 PID 输出卡前进方向 → 偏右前进)。
3. **右转改开环直驱** (`motor_enable=0` + `Motor_SetSpeed`), 右前轮闭环在负目标下因编码器噪声成"小幅正转"。
4. **`Motor_SetSpeed` 必须乘 `motor_dir`**: 右前/右后(dir=-1)的 IN1 才是倒转方向, 直接传 -200 会变前进(实测直行)。四轮实际 out=+170, 左轮前进右轮倒转 → 原地右转。
5. 90° 判据: "深离开"(在线≤2)后 "≥4路在线"(对齐竖线; 右端 ls[0] 实测不亮, 不能用中心+两端); 最小旋转1.5s; 4s 兜底; 8s 安全超时。
6. 速度 200→170 减小刹车过冲 ("多右一点点"标定)。

标定: 转多了降 `DOCK_PIVOT_SPD`, 转少了升; 若主停因是 4s 兜底则调 `DOCK_PIVOT_CAP` (角度≈时间×速度)。

### 无人值守加固 (2026-06-23, 无串口调试场景)

- **dock_state 2 找码 30s 总超时**: 找不到 AprilTag ID=1 时不再无限扫摆卡死, 自动放弃对接恢复巡线 (对接跳过, 工位流程照常跑, 无拖挂盲放)。
- **LED 阶段指示 (GPIOF Pin9, 无串口可看进度)**: `flow_stage` 1=巡线(慢闪500ms) → 2=对接(快闪100ms) → 3=工位(中闪250ms) → 4=全部完成(常亮)。
- **所有环节均有超时兜底**: 右拐3s、找码30s、识别/定位/对准各15s/15s/5s、机械臂动作30s, 任何一步失败都会降级继续, 不会永久卡死。

### Station 0 识别修复 (2026-06-23, 上电自动跑全流程识别的前提)

原 STATION_MODE 里 **Station 0 发 `MODE,AI` 但 case1 识别/定位阶段只读 `$TAG` 色块数据** (COLOR 模式输出), 而 AI 模式 OpenMV 只发 `$CLS` 分类(无位置) → Station 0 的"识别色卡→定位→对准"会**全部超时, 变成盲抓**。已修复:
- case 0 Station 0 分支 + case 8 Station 0 下一件分支: 统一改为 `MODE,COLOR,x` + 设置 `qr_target_color` + 腰座到识别位 (`identify_waist`), 与 Station 1 完全一致。
- 识别流程: 90°识别色卡(COLOR) → 170°定位色块 → QuickAlign → 抓取 → 放从车。`$CLS` AI 分类目前在 `#if !STATION_MODE` 分支保留 (答辩素材)。



原 `dock_state==1` 右拐判定有两个致命问题, 已修复:
1. **传感器索引错误**: 查 `ls[3]||ls[4]` (line_sensor.c 里是 LEFT2/LEFT1), 注释却写 RIGHT2/RIGHT1。右转弧线中横线从**右臂**扫过传感器, 右侧其实是 `ls[0]`(RIGHT1=PA4)/`ls[1]`(RIGHT2=PC3)。查左传感器 → 永远等不到"进右线" → 3s 超时, 对接卡死。
2. **起步瞬间误触发**: 转弯刚开始 5 路全在十字横线上, 直接判"进右线"立即为真 → 根本没拐, 直行穿十字路口。

修复: 改查 `ls[0]||ls[1]` + 起步保护 `DOCK_TURN_GUARD_TICKS=15` (前 150ms 不判压线, 且要求 `online<5` 已离开十字路口); 超时后复位 `dock_state=0` + `trailer_dock_flag=0`, 下个十字路口自动重试, 不再静默卡死。右拐速度仍为 左+180/右−40 (转弯半径 ≈0.32×轮距, 可画弧拐 90°)。

---

## 从车对接实机验证 (2026-08-24) ✅ 调参通过

**状态**: dock_tune 自动循环连续 3 次 `OK reached` (40cm 到位), 对接闭环稳定。

### 最终生效参数 (已烧录验证)
| 参数 | 值 | 说明 |
|------|-----|------|
| 视觉伺服目标距离 | **40cm** (VisualServo_Init) | 触发到位/挂钩距离 |
| 视觉伺服 base | 160 | 全速倒车 |
| 横向 PID (lat) | **P-only kp=1.0** (I/D=0) | 轮子差速转向 |
| 纵向 PID (long) | **P-only kp=1.5** (I/D=0) | 倒车速度 |
| 目标 cx | 160 | 图像中心 |
| 右后轮补偿 | **DOCK_RR_PCT=7%** | 开环减速补偿 |
| 右前轮补偿 | **DOCK_RF_PCT=15%** | 低速出力不足补偿 (仅倒车) |
| 降速段保底速度 | **-70** | 防止过不了电机死区 |
| 速度曲线 | 线性减速 | dist≥45 全速 / 40-45 线性 / ≤40 停 |
| 丢码策略 | **丢码即刹车** (miss>250) | 不滑行, 回找码 |

### 本轮修掉的坑 (重要经验)
1. **OpenMV 固件禁用 `image.TAG36H11` 显式引用** — 固件未 `import image`, 会抛 "name 'image' isn't defined" → 主循环 catch → mode=IDLE → 只发 $HB 不发 $TAG (vstat 可见 HB=697 TAG=0)。`find_apriltags()` 默认 family 就是 TAG36H11, 不要显式传。
2. **禁用 normalize()/median_filter** — 实测破坏 AprilTag 精确边界导致漏检。
3. **速度曲线断崖** — 原逻辑 dist 38↔39cm 速度 -160→-4.5 断崖, 车"突进→停"; 改线性减速。
4. **倒车安全钳制** — right=forward+turn 在 forward 小时 turn 会把轮子推成正转(前进), 车斜着前拱; 钳制 |turn|≤|forward|, 转向需求大时 forward 归零原地转。
5. **丢码滑行会带偏** — 删掉 coast 滑行, 丢码即停。
6. **降速段保底 -30 太低** — 被 turn 分走后过不了死区, 提到 -70。
7. **st=2 每次扫摆重发 MODE,APRILTAG** — 心跳活着≠模式正确, 只靠掉线检测永远等不到 $TAG。
8. **st=3 腰座等待** — dock_tune/dock_test 直进 st=3 时腰座还在转(5→180°), 等待到位再开始倒车计时。

### ✅ 最终确认形态 (2026-08-24, 以用户参数表为准)

**倒车对接最终调好形态**（`dock_state 3` 与起步首帧一致，差速 + 双轮补偿，勿改回"强制直线"或"开环"）：

```c
motor_enable = 1;                                        // 闭环(用户明确否决开环: "倒车不能开环,我是视觉伺服pid")
work_mode = MODE_MANUAL;
VisualServo_TaskReverse(&visual_servo, &omv_d, target_speed);
target_speed[3] += (int16_t)((-target_speed[3]) * DOCK_RR_PCT / 100);   // 右后轮减速 7% (开环, 偏快)
if (target_speed[1] < 0)
    target_speed[1] += (int16_t)(target_speed[1] * DOCK_RF_PCT / 100);  // 右前轮加力 15% (低速过死区)
```

**参数汇总 (勿再改)**：
- 视觉伺服: 目标 40cm / base 160 / 横向 P=1.0 / 纵向 P=1.5 (均 P-only) / cx=160
- 补偿: `DOCK_RR_PCT=7` (右后减速) + `DOCK_RF_PCT=15` (右前加力)
- 速度曲线: ≥45cm 全速 -160 / 40-45 线性→保底 -70 / ≤40 停
- 倒车安全: forward 单向≤0, |turn|≤|forward| 钳制, 横向死区 ±3px
- 丢码: miss>100 (1s) 刹车回找码; ≤40cm 丢码直接判到位
- 调参循环 (state 6): 前进 120, 右后 112 (=120×93%, 7% 减速)
- 右拐: `DOCK_PIVOT_SPD` 现场按角度微调 (215→200), `DOCK_PIVOT_CAP=330`

**✅ 最终方案 (2026-08-24 实测一次挂钩成功)**：**视觉伺服主回路保留 + 内环开环** — `DOCK_INNER_OPENLOOP=1` (main.c)。即 `VisualServo_TaskReverse` 双 PID 照常算 target_speed，但跳过编码器速度内环，target_speed 直接 `Motor_SetSpeed(motor_dir×target)` 映射 PWM (同右拐 dock_state 1 的直驱方式)。原因: 编码器方向间歇性乱 (接触不良), 速度环正反馈 → 车原地转/不后退; 开环后编码器不再干扰, 视觉伺服靠 RR/RF 补偿保证左右一致性。

**已否决的方向 (实测记录, 勿再试)**：差速被否后曾改"强制直线+腰座对准" (009d4be0 试过)、RR 改 15%/25% 加大 (右后死区不动)、整体开环(不用视觉伺服) (用户否决) — 注意"内环开环"≠"整体开环": 内环开环保留视觉伺服主回路, 是最终方案。

**⚠️ 勿改 encoder_dir 全局方向 (2026-08-24 教训)**：`encoder_dir={-1,-1,-1,-1}` 是**全局**的 (巡线速度环也用), 曾误以为左前/左后倒车 real 为正=方向反, 改成 `{1,-1,1,-1}` → **巡线左轮失控乱跑**。已撤销。倒车 real 时正时负 (motor0 从 -228→+73→+84) 说明是**编码器间歇性接触不良** (方向随机跳), 不是固定反向 → 固定改 encoder_dir 无效且破坏巡线, **只能查硬件接线/供电**。

**⚠️ 逻辑冲突修复 (2026-08-24, "对接时车往前跑")**：
1. **AI 流水线与 dock 状态机无互斥** — 原 `if (!dock_tune_on)` 不检查 dock_state, dock 收尾切 QRCODE 后 AI 立即被 QR 触发 approach 前进, 抢 motor/OpenMV → 车往前跑。已改 `if (!dock_tune_on && dock_state == 5)` (AI 只在 dock 完成后激活)。
2. **丢码刹车阈值** — 原 `miss > 250`(2.5s 滑行) → 先改 `miss > 30`(0.3s) 太紧: OpenMV APRILTAG ~200ms/帧 + 偶发延迟 → 正常帧间隙被误判丢码, 车刚起步就刹 → "不往后走"。**最终 `miss > 100` (1s)**: 覆盖 5 帧+延迟不误判, 真丢码 1s 刹。

**编码器提示 (已绕过)**: 倒车斜/不后退的**根因是编码器读数不可靠** (RDK real 三路时正时负/幅度爆炸, 右拐 enc 有时 0 有时 6 万, 方向随机跳 = 接触不良)。已用"内环开环"绕过, **对接不再依赖编码器**。但巡线 (LINE_FOLLOW) 仍走速度环 — 若日后巡线也打摆/走不直, 才是编码器连前进方向也影响了, 需查那几路接线/供电。

### OpenMV 固件 (H盘 main.py)
- `TAG_W_CM = 10.0` (10cm 码), `FOCAL_TAG = 282` (纯焦距, 与码尺寸无关)
- `run_apriltag()`: `img.find_apriltags()` 无参调用 (默认 TAG36H11)
- 上电默认 QRCODE 模式, 需 `MODE,APRILTAG` 切换

### 硬件缺陷 (已知)
- Motor 3 (右后) 编码器损坏 — 开环
- Servo ID 3 (夹爪) — 可用
- OLED 未接 — OLED_Init 已注释
- 蓝牙未用 — BT_Init 已注释

---

## 待办 (从车对接)

### 从车硬件
- [x] 制作从车底盘 (木板/亚克力 + 2 个被动轮)
- [x] 从车前方安装 AprilTag (打印 A4 纸) — **现用 10cm×10cm 码**
- [x] 从车前方开两个槽口, 供主车夹爪伸入夹持
- [x] AprilTag ID 与主车约定一致 (ID=1, tag36h11)

### 从车对接软件
- [x] main.c 启动时先跑从车对接 (TaskQueue 第 1 步) — 已实现 dock_state 状态机 (十字路口→右拐→找码→倒车→挂钩)
- [x] 对接成功后 trailer_docked=1, 切到货物收集循环
- [x] 货物收集完毕, TaskQueue 跑终点
- [x] ACTION_HOOK 实现 (夹爪伸入从车槽口并夹紧) — Action_HookTrailer()
- [x] 视觉 PID 实机调参 (vkp/vki/vpid 命令) — **对接已改视觉伺服式**: dock_state 3 倒车用 VisualServo_TaskReverse 双 PID (横向 P-only kp=1.0 + 纵向 P-only kp=1.5, 右后轮 7% + 右前轮 15% 补偿, 距离跳变过滤, **丢码即停**) — **2026-08-24 连续 3 次 OK reached 40cm 验证通过**
- [x] AprilTag 尺寸: **已从 15cm 大码换为 10cm 码** — OpenMV TAG_W_CM=10.0 (FOCAL_TAG=282 纯焦距不变, 与码尺寸无关)

### LLM 自动调参 (从车对接完成后)
- [ ] visual_servo.c 加 CSV 遥测输出
- [ ] 跑 LLM PID Tuner 精调参数
- [ ] 录屏: LLM 自动调参过程 → 放设计报告

### 设计报告 + 答辩
- [ ] 撰写设计报告
- [ ] 答辩 PPT: 控制链路一张图 + AI 全链路 (采集→训练→量化→部署)
- [ ] 预演答辩 10 遍
- [x] 现场备用: 串口 `run`/`restart` 一键重置整流程命令 (已实现, 2026-06-23)

### 答辩应用场景（重点讲中间两个）

| 场景 | 对应能力 | 答辩策略 |
|------|---------|---------|
| 3C电子产线 | 巡线零成本改线 | 略提 |
| **医院物流** | 端侧AI离线可用，不依赖WiFi（医疗设备电磁干扰） | **重点展开** |
| **快递中转场** | 视觉伺服自动挂接从车，无动力拖挂成本极低 | **重点展开** |
| 危险品仓库 | 本地推理+电池供电，防爆区无布线 | 略提 |

**场景一：医院物流**
- 药品/标本配送，不能走WiFi（电磁干扰医疗设备）
- 端侧AI不依赖网络，离线可跑
- 巡线黑胶带部署灵活，不破坏医院地面
- 系统四种硬件容错策略 → 医疗场景对可靠性的高要求

**场景二：快递中转场**
- 拖挂多节笼车，人工对接费力且易出错
- 视觉伺服自动挂接从车，替代人工对接
- 十字路口检测 → 自主对接 → 拖挂运输 → 全流程无人化
- 无动力从车成本极低（木板+被动轮+AprilTag打印纸），适合大规模部署

---

## 最终演示流程 (~2.5 分钟)

```
上电
 │
 ├─ 0:00  ★ 从车对接 (开场高光)
 │        巡线 → 十字路口(5路全踩) → 停车
 │        → 旋转扫描 → 扫到从车 AprilTag (ID=1)
 │        → 旋转对准 → 直进靠近 → 夹爪伸入槽口
 │        → LED 长亮 = 挂接成功
 │        评委第一印象: 十字路口定位 + 视觉对接, 不一样
 │
 ├─ 0:30  拖着从车巡线 → 找货物 1 (红五边形)
 │        QR扫到 → AI分类 "red_hexagon 95%"
 │        → 腰座+车身对准 → 夹爪抓 → 放从车上
 │
 ├─ 1:00  货物 2 (绿圆形)
 │        冷却期后自动触发 → 同上
 │        评委看到: 循环可靠
 │
 ├─ 1:30  货物 3 (黄长方形)
 │        同上 → 从车上堆了三个货
 │
 ├─ 1:50  拖着满载从车巡线到终点
 │
 └─ 2:10  停车 → LED 三闪 = 任务完成
```

## 从车对接详细设计

### 触发: 巡线十字路口检测

利用现有 5 路巡线传感器, 十字路口时全部踩线:

```
正常巡线:                   十字路口:

  ██                         ██
  ██ ← 2-3 路踩线             ██
──██──                     ──████──
  ██                         ██
  ██                     5 路全踩 → 触发停车

LineSensor_AllOn() → TIM6 ISR 自动停车 → main loop 接管
```

### 对接: 序贯对准 (先转再直)

```
十字路口停车
    │
    ▼
OpenMV 切 APRILTAG 模式
    │
    ▼
旋转扫描: 车身原地旋转一圈找 AprilTag
    │  左轮正转 + 右轮反转 = 原地旋转
    │  扫到从车标签 (tag_id=1) → 记录
    │
    ▼
旋转对准: 比例P控制
    │  error_x = cx - target_cx
    │  turn = error_x × 3  (±400钳制)
    │  |error_x| < 15px → 对准完成
    │
    ▼
直进靠近: 比例P控制
    │  forward = (distance - target_dist) × 20  (±300钳制)
    │  distance ≤ target_dist + 3cm → 停止
    │
    ▼
停车 → 夹爪伸入从车槽口 → 挂接完成
```

> **设计决策**: 先转再直而不是双PID同时跑。现场比赛稳 > 快, 序贯方式各阶段独立调参, 互不干扰, 翻车概率几乎为零。双PID同时跑的代码保留在 visual_servo.c 作为答辩素材, 证明我们实现了两种策略并选择了更可靠的方案。

### Tag 安装: 垂直贴, 与摄像头同高

```
主车                          从车
┌──────┐                    ┌──────────┐
│      │                    │          │
│  📷  │ ← 摄像头高度 h     │  标签    │ ← 高度 = h
│      │                    │   ⬜     │
│ 夹爪 │ ←── 对接 ──→      │  槽口    │
└──────┘                    └──────────┘
```

---

## 实现方案: TaskQueue + AI Pipeline 混合调度

```
main() 启动 → 巡线出发 (OpenMV=APRILTAG)
    │
    ▼
十字路口检测 (5路全踩)
    │
    ▼
停车 → 旋转扫描 → 对准 → 夹爪进槽口 → trailer_docked=1
    │
    ▼
倒车退回巡线 → 切 OpenMV=QRCODE
    │
    ▼
AI Pipeline 货物收集 (现有 main.c Phase 0-3 循环)
    │  巡线 → QR → AI分类 → 颜色对准 → 抓取 → 放从车
    │  重复 3 次 (红→绿→黄)
    ▼
巡线到终点 → 停车 → LED 三闪
```

### 要改动的文件
| 文件 | 改动 | 工作量 |
|------|------|--------|
| line_sensor.c/h | 新增 `LineSensor_AllOn()` 函数 | ~8 行 |
| Timer.c | ISR 里巡线分支加入十字检测, 自动停车 | ~8 行 |
| main.c | 十字触发后接管: 旋转扫描→对准→直进→夹取 | ~40 行 |
| main.c | 对接成功后启动 AI Pipeline | ~5 行 |
| main.c | 货物完成 → 巡线到终点 | ~5 行 |
| state_machine.c | ACTION_HOOK 实现 | ~5 行 |
| OpenMV main.py | 不改, 已支持 APRILTAG 模式 | 0 |

### 备用串口命令
```
restart  — 一键重置: 无论在哪个 phase, 直接回到巡线继续走
vpid     — 查看视觉伺服 PID 当前参数 + 对准状态
```

---

## 类别→颜色映射

| class_id | 类别 | 颜色 ID | 用途 |
|:--:|------|:--:|------|
| 0 | red_hexagon | 1 (红) | AI分类后颜色对准 |
| 1 | green_circle | 2 (绿) | |
| 2 | yellow_rect | 3 (黄) | |

## QR 码约定
| 内容 | 动作 |
|------|------|
| 包含 `GRASP` | 抓取 |
| 包含 `PLACE` | 放置 |
| 其他/空 | 默认抓取 |

---

## 答辩创新点待办 (2026-06-30)

### 🟡 中投入（答辩前做完）

#### 1. 自适应 PID — 根据从车负载自动调增益
- [ ] `Timer.c` 或 `pid.c`：根据 `trailer_cargo_count` 动态调整速度 PID 的 Kp
- [ ] 空载 vs 满载查表/线性插值（质量比 1:4 → Kp 波动 ~30%）
- [ ] 答辩说法："拖挂三个货物后整车质量变化 ~3 倍，做了负载自适应 PID"

**实现思路**：
```c
// 在 TIM6 ISR 的速度 PID 计算前
float mass_ratio = (trailer_cargo_count + 1.0f) / 4.0f;  // 0.25 ~ 1.0
float kp_adj = Kp_base * (1.0f + mass_ratio * 0.3f);
PID_SetKp(&pid_motor[i], kp_adj);
```

#### 2. 抓取成功率统计 + 可视化
- [ ] `main.c` 或 `vision_task.c`：每次抓取后记录成功/失败
- [ ] 按货物类别分别统计（红六边形/绿圆形/黄长方形）
- [ ] 串口命令 `stats` 输出统计结果
- [ ] 跑 ≥30 次实验，画柱状图放 PPT

**数据结构**：
```c
struct {
    uint16_t attempt[4];   // [0]=total, [1]=red, [2]=green, [3]=yellow
    uint16_t success[4];   // 同上
} grasp_stats;
```

#### 3. 舵机掉电策略 — 能耗量化分析
- [ ] 估算各舵机保持/运动电流（查 MG996R 或同类 datasheet）
- [ ] 测算到达目标后断电节省的功耗（已有代码，缺数据）
- [ ] PPT 上放一张对比表：持续供电 vs 智能断电的续航差异

**答辩说法**："腰座舵机保持力矩需要持续 ~300mA@6V，到达目标后断电节省 ~1.8W，三个舵机累计续航提升约 15%。"

---

### 🔴 高投入（答辩后有空做，可写进设计报告）

#### 4. AprilTag 里程计融合定位
- 赛道关键位置贴 AprilTag 作为绝对地标
- 路过时用标签位姿校正编码器里程计累积误差
- 本质是简化版 EKF/互补滤波 —— 地标辅助里程计

#### 5. 从车路径规划 — 铰接车运动学
- 拖挂从车后转弯半径变化建模（单车 → 铰接车）
- 论文/报告里放运动学公式 + 最小转弯半径推导
- 可选：倒车入库的路径规划仿真

#### 6. AI 模型鲁棒性测试 — 对抗样本实验
- 对测试图片加噪声（亮度 ±30%、旋转 ±15°、遮挡 20%）
- 测试 MobileNetV2 α=0.35 在扰动下的精度衰减曲线
- 学术界标准实验方法，图多、花钱少、放设计报告很加分
